import { LinkIcon } from "lucide-react";

const Logo = () => {
    return (
        <div className="border-secondary from-primary via-primary/70 to-primary flex h-10 w-10 items-center justify-center rounded-full border bg-gradient-to-tr p-2 text-white">
            <LinkIcon className="" />
        </div>
    );
};

export default Logo;
